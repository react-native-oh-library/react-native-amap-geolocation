import { LogUtil } from './utils/LogUtil'; import { Constants } from './utils/Constants'; const a10 = 'AMapResourceManager'; export class AMapResourceManager { constructor(context) { this.context = context; } getRawfileContent(path) { let content = new Uint8Array([]); try { content = this.context.resourceManager.getRawFileContentSync(path); } catch (error) { let code = error.code; let message = error.message; LogUtil.e(Constants.z9, a10, `getRawFileContentSync failed ${path}, error code: ${code}, message: ${message}.`); } return content; } } 