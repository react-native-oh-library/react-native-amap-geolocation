import systemDateTime from '@ohos.systemDateTime'; import { LogUtil } from './LogUtil'; export class d27 { static v36(isNano = false) { try { let g42 = systemDateTime.getCurrentTime(); return g42.then((time) => { return new Promise((j42) => { let k42 = time.toString(); if (k42.length > 3) { k42 = k42.slice(0, k42.length - 2) + d27.b37 + k42.slice(k42.length - 1); j42(k42); } else { j42(''); } }); }); } catch (e) { let error = e; LogUtil.e('TimeUtil', 'Failed to get currentTime. message', `${error.message}, code: ${error.code}`); return new Promise((f42) => { f42(''); }); } } static async w26() { try { let y41 = systemDateTime.getCurrentTime(); return y41.then((time) => { return new Promise((b42) => { var c42; let d42 = (c42 = time.toString()) !== null && c42 !== void 0 ? c42 : ''; b42(d42); }); }); } catch (e) { let error = e; LogUtil.e('TimeUtil', 'Failed to get currentTime. message', `${error.message}, code: ${error.code}`); return new Promise((x41) => { x41(''); }); } } } d27.b37 = '1'; export default d27; 